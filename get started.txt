git clone <your-repo-url>

cd test-automation && ls -la — inspect files

cd unizo-e2e-automation

Create fixtures & suites (the cat > ... blocks from script)

npm ci (or npm install)

If docker-compose present: docker-compose up -d then run:

docker exec -it <container> bash -lc "cd unizo-e2e-automation && npm run validate && npm run test:dev test-definitions/suites/auth/login.yml"

Else run: npx cypress open (headed) or npx cypress run --spec "cypress/e2e/authored/login.cy.ts" --env username="...",password="..." 